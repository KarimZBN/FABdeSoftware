body {
    font-family: Arial, sans-serif;
    background-color: #f4f4f4;
    margin: 0;
    padding: 10px;
}

h1 {
    color: #333;
    font-size: 20px; /* Tamanho da fonte reduzido */
}

form {
    background-color: #fff;
    padding: 10px; /* Reduzir padding */
    border: 1px solid #ccc;
    border-radius: 4px;
    max-width: 400px; /* Largura máxima do formulário */
    margin: 0 auto; /* Centralizar o formulário */
}

label {
    display: inline-block;
    width: 100px;
    font-size: 12px; /* Tamanho da fonte reduzido */
}

input[type="text"],
input[type="number"],
input[type="checkbox"] {
    width: calc(100% - 110px);
    padding: 3px; /* Reduzir padding */
    font-size: 12px; /* Tamanho da fonte reduzido */
    margin-bottom: 10px; /* Reduzir o espaçamento entre os campos */
    height: 20px; /* Limitar a altura */
}

button {
    background-color: #5cb85c;
    color: #fff;
    border: none;
    padding: 5px 10px; /* Reduzir padding */
    font-size: 12px; /* Tamanho da fonte reduzido */
    cursor: pointer;
    height: 30px; /* Limitar a altura */
}

button:hover {
    background-color: #4cae4c;
}
